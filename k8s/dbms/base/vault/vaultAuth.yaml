apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: iris-dbms-vault-auth
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: iris-dbms-role
    serviceAccount: iris-dbms-sa
    audiences:
      - vault
